PROGRAM Test;

CONST
    FACTOR = 8;
    epsilon = 1.0e-6;

TYPE
    arr = array[boolean] of array[char] of char;



VAR
    i, _var, xyz : integer;

PROCEDURE p(j : real);
  VAR k : char;

  FUNCTION f(x : real) : real;
    VAR i:real;

    BEGIN {f}
      f := i + j + n + x;
    END {f};

  BEGIN {p}
    k := chr(i + trunc(f(n)));
  END {p};

BEGIN

  arr[45] := 1;
  
END.